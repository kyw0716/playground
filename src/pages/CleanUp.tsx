import { CleanUp } from '../components/cleanUp';
import { Layout } from '../components/layout/Layout';

export const CleanUpPage = () => {
  return (
    <Layout title="Clean up">
      <CleanUp />
    </Layout>
  );
};
